name: daily
on:
  schedule:
    - cron: '* */24 * * *'
  workflow_dispatch:

jobs:
  setup:
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os:
          - ubuntu-latest
          - macos-latest
    steps:
      - run: curl -sSLf setup.kmdk.uk | bash
